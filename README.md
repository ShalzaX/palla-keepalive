# Palla Keep-Alive

Public repo. Contains only a GitHub Actions cron that pings
`https://palla-trade-bot.onrender.com/health` every 5 minutes so the
Render free-tier Web Service never hits its 15-minute HTTP-idle spin-down.

Bot code is in the private repo `ShalzaX/palla-trade-bot`.
